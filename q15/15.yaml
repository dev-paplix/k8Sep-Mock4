apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: api-server
  name: api-server
  namespace: production
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api-server
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: api-server
    spec:
      containers:
      - image: nginx:1.18
        name: nginx
        resources:
          limits:
            cpu: 500m
          requests:
            cpu: 200m
status: {}